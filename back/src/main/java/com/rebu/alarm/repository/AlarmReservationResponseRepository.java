package com.rebu.alarm.repository;

import com.rebu.alarm.entity.AlarmReservationResponse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlarmReservationResponseRepository extends JpaRepository<AlarmReservationResponse, Long> {
}
